# Citable API

This is the lightweight MVP backend for Citable.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

## Local environment

Copy `.env.example` to `.env` and update values as needed.

## Notes

- This API intentionally keeps the architecture lightweight for the MVP.
- It exposes a demo audit endpoint to support the onboarding flow and dashboard.
